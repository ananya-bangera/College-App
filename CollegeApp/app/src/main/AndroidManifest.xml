<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.collegeapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeApp">
        <activity android:name=".ebook.PdfViewerActivity"></activity>
        <activity android:name=".ebook.EbookActivity" />
        <activity android:name=".image.DisplayImageActivity" />
        <activity android:name=".notice.DeleteNoticeActivity" />
        <activity android:name=".DisplayNotice" />
        <activity android:name=".faculty.UpdateTeacherActivity" />
        <activity android:name=".faculty.AddTeachers" />
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".ebook.UploadPdfActivity" />
        <activity android:name=".image.UploadImage" />
        <activity android:name=".notice.UploadNotice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>